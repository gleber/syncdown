---
"@syncdown/cli": minor
"@syncdown/tui": minor
---

feat(cli): headless config management with TUI parity — `config show`/`config get`, guided `connect google|notion|todoist` OAuth/token flows, `disconnect`, and `calendars` listing
