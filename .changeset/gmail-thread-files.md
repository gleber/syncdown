---
"@syncdown/cli": minor
---

feat(gmail): merge email threads into one markdown file per thread, bucket messages into per-day directories, and prefix filenames with the received hour (existing gmail state auto-resets and re-syncs under the new layout)
